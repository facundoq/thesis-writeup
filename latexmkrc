$out_dir= "build";
$pdf_mode=1;
$preview_continuous_mode=1;
$silent=1;
#-quiet -silent -c-style-errors
$pdflatex_silent_switch = "-file-line-error -halt-on-error ";

#$pdflatex_silent = "pdflatex -quiet -file-line-error -interaction=nonstopmode -c-style-errors"
#$latex_silent_switch = $pdflatex_silent_switch;
# $pdflatex_default_switches = '';
